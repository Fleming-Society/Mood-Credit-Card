# Firmware examples

Small, self-contained examples used by historical teaching sessions belong
here. Each Arduino sketch must stay in a directory that matches its `.ino`
filename.

The recovered Session 2 examples demonstrate I2C communication between an
ESP8266 master and an Arduino Uno slave. They have not been compiled recently.
